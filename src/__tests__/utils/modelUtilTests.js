require('source-map-support').install();
const expect = require('chai').expect;
import * as Util from '../../utils/model';

describe('something', function () {
    it('should do something', function () {

        expect(true).to.be.true;
    });
});